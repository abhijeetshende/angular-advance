import { StructraldirectiveDirective } from './structraldirective.directive';

describe('StructraldirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StructraldirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
