import { Highlight3Directive } from './highlight3.directive';

describe('Highlight3Directive', () => {
  it('should create an instance', () => {
    const directive = new Highlight3Directive();
    expect(directive).toBeTruthy();
  });
});
